# -WSQ08
#include <iostream>
using namespace std;

  int suma(int a, int b){

      return a+b;
  }

  int resta(int a, int b){
      return a-b;
  }

  int multiplicacion(int a, int b){
      return a*b;
  }

  int division(int a, int b){
      return a/b;
  }

  int residuo(int a, int b){
      return a%b;
  }

  int main(){

    int x, y;

    cout<< "Introduce un número" << endl;
    cin >> x;
    cout<< "Introduce otro o el mismo número" << endl;
    cin >> y;
    

    cout << "La suma de " << x << " + " << y << " es igual a: " << suma(x,y) << endl;
    cout << "La resta de " << x << " - " << y << " es igual a: " << resta(x,y) << endl;
    cout << "La multiplicación de " << x << " * " << y << " es igual a: " << multiplicacion(x,y) << endl;
    cout << "La división de " << x << " / " << y << " es igual a: " << division(x,y) << endl;
    cout << "El residuo de " << x << " / " << y << " es igual a: " << residuo(x,y) << endl;


return 0;
}
